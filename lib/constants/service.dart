const Map<String, String> jsonHeaders = {
  'Content-type': 'application/json',
  'Accept': 'application/json',
};

const Map<String, String> formHeaders = {
  'Content-type': 'multipart/form-data',
  'Accept': 'application/json',
};

const baseUrl = 'pure-shelf-07661.herokuapp.com';
const urlLocation = 'locations';
const urlUser = 'users';
const urlApartment = 'apartments';
