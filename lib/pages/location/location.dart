import 'package:flutter/material.dart';

class LocationUserPage extends StatelessWidget {
  const LocationUserPage({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Location'));
  }
}

class LocationManagerPage extends StatelessWidget {
  const LocationManagerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

class LocateApartmentPage extends StatelessWidget {
  const LocateApartmentPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
